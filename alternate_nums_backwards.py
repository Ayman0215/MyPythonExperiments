#----------------------------------------------------------------#
def reverseList(nums):
    index = len(nums) - 1
    while index >=0:
        print(nums[index])
        index -=2
    return None
#----------------------------------------------------------------#
